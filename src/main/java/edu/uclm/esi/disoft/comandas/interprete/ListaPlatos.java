package edu.uclm.esi.disoft.comandas.interprete;
import edu.uclm.esi.disoft.comandas.dominio.Plato;

public class ListaPlatos {
	private Plato plato;
	private ListaPlatos listaPlatos;
	
/*public ListaPlatos(String[] tokens, int index) throws Exception {
		String mensajeError="Esperaba ( ENTERO, CADENA, ENTERO )"; 
		// ( 2, Tortilla , 1 ) , ( 1, Gazpacho, 3 )
		if(tokens.length<7) 
			throw new Exception(mensajeError);
		
		if(!tokens[index].equals("(")) 
			throw new Exception(mensajeError);
		
		int unidades;
		try {
			unidades=Integer.parseInt(tokens[index+1]);
		} catch {
			
		}
	}*/
}
